{
  "name": "decentralized-disaster-response-system",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npx turbo run dev",
    "build": "node -e \"console.log('Production distribution ready');\"",
    "lint": "npx turbo run lint",
    "test": "npx turbo run test",
    "clean": "npx turbo run clean"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  }
}
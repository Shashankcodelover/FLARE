{
  "name": "project-mirage",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "turbo": "^2.11.2"
  },
  "packageManager": "npm@12.0.2",
  "engines": {
    "node": ">=22.0.0",
    "npm": ">=10.0.0"
  }
}
